import 'package:flutter/material.dart';

class SellProduct extends StatelessWidget {
  const SellProduct({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("SellProduct"),
    );
  }
}